name: Check HTML robots protection
on: [push, pull_request]
permissions:
  contents: read
jobs:
  robots:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test tests/robots.test.js
